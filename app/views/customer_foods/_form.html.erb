<%= form_for(@customer_food) do |f| %>
  <% if @customer_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_food.errors.count, "error") %> prohibited this customer_food from being saved:</h2>

      <ul>
      <% @customer_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.text_field :customer_id %>
  </div>
  <div class="field">
    <%= f.label :food_id %><br>
    <%= f.text_field :food_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
